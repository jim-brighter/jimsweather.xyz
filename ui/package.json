{
  "scripts": {
    "build": "node build.js",
    "start": "http-server -p 8080 -c-1 -o",
    "start-prod": "http-server -p 8080 -c-1 -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
