{
  "scripts": {
    "build": "node build.js",
    "start": "npm --prefix ../lambda start & http-server -p 3000 -c-1 -o",
    "start-prod": "http-server -p 3000 -c-1 -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
